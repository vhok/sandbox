module.exports = {
    add: (a, b) => parseInt(a) + parseInt(b),
    subtract: (a, b) => parseInt(a) - parseInt(b)
};

// exports.add = (a, b) => {
//     return a + b;
// };

// exports.subtract = (a, b) => {
//     return a - b;
// };

// doesn't work
// exports = {
//     add: (a, b) => parseInt(a) + parseInt(b),
//     subtract: (a, b) => parseInt(a) - parseInt(b)
// };
